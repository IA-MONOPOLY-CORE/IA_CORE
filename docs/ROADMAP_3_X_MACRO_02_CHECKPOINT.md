# Roadmap 3.x Macro-Mission 02 Integral Checkpoint

`ROADMAP_3X_MACRO_02_B4_B5_PASSED_B6_CONDITIONALLY_DEFERRED_AND_PUBLISHED`

## Mission result

Macro-Mission 02 materialized Method Santi 3.2 and a governed readiness layer
without operational execution. The method remains adaptive and preserves the
previous contract-first, evidence-first, frontier, checkpoint, rollback, and
current-contract-wins rules. The complete machine-readable evidence is in
`docs/ROADMAP_3_X_MACRO_02_CHECKPOINT_EVIDENCE.json`.

## State at checkpoint

- Branch: `main`.
- Pre-checkpoint HEAD: `555f0294a5aba4027757de2f2ea40425c06bb7f5`.
- Pre-checkpoint tree: clean; local branch is 18 commits ahead of the unchanged
  `origin/main` Macro 01 checkpoint.
- Macro 01 evidence and four published commits remain intact.
- No pull, merge, rebase, reset, force operation, tag, or product migration was
  used.

## Materialized stations

1. Method Santi 3.2 additive method and focal guard.
2. Opt-in OCI resource budgets with deterministic omission reasons.
3. Macro 01 learning adjudication: six `CANDIDATE` items, no auto-promotion.
4. B4-A permission and activation lifecycle boundary.
5. B4-B provider/network/credential boundary,
   `CONTRACT_READY_EXTERNAL_EVIDENCE_PENDING`.
6. B5-A persistence ownership and retention matrix.
7. B5-B sandbox recovery and rollback matrix.
8. Route convergence: all 36 remain `UNKNOWN`.
9. Loteria P3 capability extraction without code movement.
10. B6 internal workforce readiness contract, no active workforce.
11. Macro 02 DOOL/OCI learning loop with observations preserved and no new
    candidate.

Each station has a focal test and a local Git record. The evidence JSON lists
the exact commit chain and postconditions.

## Frontier result

The mission reduced uncertainty about static contracts, resource-bounded OCI,
ownership families, sandbox recovery, workforce readiness, and domain capability
boundaries. It did not close the route-specific F-004 frontier, external
provider/credential evidence, deployment identity, tenant semantics, or legacy
product persistence ownership. No new operational frontier was hidden by a
classification upgrade.

## Safety result

HTML, JavaScript contractual surfaces, i18n, backend route behavior, payloads,
runtime, execution, endpoints, integrations, providers, secrets, product data,
network traffic, and active workforce remained untouched. Tests used repository
inspection and temporary sandbox fixtures only.

## Publication

The integral checkpoint is a documentation/test/evidence station. Its commit
hash is recorded by Git after creation. Final remote publication is allowed only
after the full validation, diff, secret, side-effect, and remote-divergence
checks required by Macro-Mission 02 pass. The in-scope new tests and safe
contract suites pass. The repository-wide baseline still has three explicitly
out-of-scope conditions: two root legacy collection errors (`test_debate.py` and
the network-calling `test_respuesta.py`) and two historical HTML assertions in
`tests/test_api_admin_panels.py`; Macro 02 did not touch any of those surfaces.
