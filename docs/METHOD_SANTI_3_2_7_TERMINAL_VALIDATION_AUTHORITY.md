# Method Santi 3.2.7 - Terminal Validation Authority

Macro-Mission 06.2 records a bounded method increment over 3.2.6:

```text
direct post-basis delta
+ index delta
+ worktree delta
+ untracked paths
+ executable diff checks
+ durable log-bound receipts
+ one terminal Level B
+ deterministic report repeatability
+ exact post-terminal allowlist
```

The increment is a closure authority, not a product capability. It preserves
historical gates byte-for-byte and keeps external enforcement denied until a
hosting operator supplies provider-originated evidence.
