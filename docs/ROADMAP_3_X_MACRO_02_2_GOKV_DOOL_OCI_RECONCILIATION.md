# Roadmap 3.x Macro-Mission 02.2 GOKV, DOOL, and OCI Reconciliation

## Disposition

Macro 02.2 produced two observations that are genuinely distinct from the
existing 32-item GOKV inventory. They are stored as `DEVELOPMENT_ORIGIN`
`CANDIDATE` items with evidence, limitations, recovery, and future validation
criteria. No automatic promotion occurred.

## Reused knowledge

- `focal_group_canonical_deep_test_policy` supplied the risk-based expansion to
  deep historical probes.
- `commit_repair_needs_explicit_parity` supplied explicit checkpoint identity
  and repair accountability.
- `contract_over_ui_inference` supplied the rule that a future surface cannot
  infer authority from presentation.
- `context_continuity_reduces_rework` supplied the continuity/provenance
  connection for future surfaces.
- `evidence_before_closure`, `station_local_commits`, and `no_fake_operational_state`
  remain governing rules; no duplicate item was created for them.

## New candidates

### `historical_checkpoint_requires_evidence_endpoint`

Historical tests are trustworthy only when source reads, fixtures, repository
state, and Git diff commands resolve to the exact checkpoint under evaluation.
The adapter may change that evidence endpoint while retaining assertions,
deny-by-default behavior, and protected paths. Current tests continue to read
the current worktree and index.

### `one_core_four_surfaces_preserves_canonical_authority`

One canonical intelligence/control core can support four environment-specific
product surfaces without four independent truths. The candidate records the
architectural rule, not an OS, mobile, runtime, provider, endpoint, or
integration capability.

## DOOL and provenance

Both items preserve `learning_origin = DEVELOPMENT_ORIGIN`, source commits,
source checkpoints, evidence references, scope, failure modes, and recovery.
The learning event and post-block loop preserve the observations without
copying private conversation state or modifying model weights. The metric keeps
unknown quota and duration values as `null`; no performance number is invented.

## OCI boundary

OCI remains development-only and governed. The two candidates are not
automatically selected for runtime or execution; they are not automatically selected
for any product path. A future mission may select
them only when they are pertinent to its explicit scope and resource, privacy,
risk, and authority boundaries. Irrelevant, stale, duplicate, private, or
out-of-scope knowledge must remain excluded with an explicit reason.

`ROADMAP_3_X_MACRO_02_2_LEARNING_RECONCILIATION_STORED`

No GOKV promotion, provider call, network call, runtime, execution, endpoint,
integration, product payload, or product persistence was performed by this
reconciliation.
